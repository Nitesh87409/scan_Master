pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "native_scanner_poc"
include(":app")
include(":opencv")
project(":opencv").projectDir = file("opencv/OpenCV-android-sdk/sdk")
